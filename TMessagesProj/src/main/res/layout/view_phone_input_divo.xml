<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/phone_scroll"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:id="@+id/root"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingTop="30dp"
        android:paddingBottom="16dp"
        android:gravity="center_horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/title"
            android:text="@string/YourNumber"
            android:textSize="20sp"
            android:textStyle="bold"
            android:layout_marginBottom="12dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/subtitle"
            android:text="@string/StartText"
            android:textSize="14sp"
            android:alpha="0.8"
            android:layout_marginBottom="20dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <!-- Блок кода страны + номер -->
        <LinearLayout
            android:id="@+id/phone_block"
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <EditText
                android:id="@+id/country_code"
                android:layout_width="72dp"
                android:layout_height="56dp"
                android:inputType="phone"
                android:hint="+374"
                android:maxLines="1"
                android:gravity="center_vertical"
                android:imeOptions="actionNext" />

            <EditText
                android:id="@+id/phone_input"
                android:layout_width="0dp"
                android:layout_height="56dp"
                android:layout_weight="1"
                android:inputType="phone"
                android:hint="@string/PhoneNumber"
                android:maxLines="1"
                android:imeOptions="actionDone" />
        </LinearLayout>

        <TextView
            android:id="@+id/helper"
            android:text="test"
            android:textSize="12sp"
            android:alpha="0.7"
            android:layout_marginTop="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <!-- Необязательная локальная кнопка "Далее".
             FAB/Done от хоста все равно будет работать. -->
        <Button
            android:id="@+id/next_local"
            android:text="@string/Next"
            android:layout_marginTop="20dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone"/>
    </LinearLayout>
</ScrollView>
